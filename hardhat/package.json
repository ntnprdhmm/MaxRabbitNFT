{
  "name": "@max-rabbit-nft/hardhat",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx -p hardhat hardhat node",
    "deploy": "npx -p hardhat hardhat run --network localhost scripts/deploy.js",
    "clean": "npx -p hardhat hardhat clean",
    "compile": "npx -p hardhat hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.1",
    "@openzeppelin/contracts": "^4.8.0",
    "@typechain/ethers-v5": "^10.1.1",
    "@typechain/hardhat": "^6.1.4",
    "hardhat": "^2.12.4",
    "typechain": "^8.1.1",
    "dotenv": "^16.0.3"
  }
}
